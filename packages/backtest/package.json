{
  "name": "@quant/backtest",
  "version": "1.0.0",
  "description": "事件驱动回测引擎 - Event-Driven Backtesting Engine",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch",
    "clean": "rimraf dist .tsbuildinfo",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@quant/exchange": "workspace:*",
    "@quant/marketdata": "workspace:*",
    "decimal.js": "^10.4.3",
    "eventemitter3": "^5.0.1",
    "pino": "^8.18.0"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "typescript": "^5.4.0",
    "vitest": "^1.3.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
